import Info from './info';
import Project from './project';
import Settings from './settings';

import * as RichText from './rich-text';

const Schemas = [Info, Project, RichText.Base, RichText.Simple, Settings];

export default Schemas;
